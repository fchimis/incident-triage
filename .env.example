# Copy to .env and fill in.
# .env is gitignored; .env.example is committed.

GEMINI_API_KEY=your-key-here

# Optional: override the default model if Google renames the current
# generation. Leaving this blank uses the client's built-in default.
# GEMINI_MODEL=gemini-3.6-flash
